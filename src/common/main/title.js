import React, {PropTypes as T} from 'react'

const MainTitle = (props) => 
	<h1>This is Pure.CSS!</h1>

MainTitle.propTypes = {/* propTypes goes here */}
MainTitle.defaultProps = {/* defaultProps goes here */}

export default MainTitle
